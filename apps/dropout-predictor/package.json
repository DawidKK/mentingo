{
  "name": "dropout-predictor",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn dropout_predictor.main:app --host 0.0.0.0 --port 8000 --reload",
    "train": "uv run python train.py",
    "generate:data": "uv run python generate_data.py",
    "test": "uv run pytest",
    "lint": "uv run ruff check ."
  }
}
